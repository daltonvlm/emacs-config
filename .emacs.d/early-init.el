;;; early-init.el --- -*- lexical-binding: t -*-

;;; Commentary:
;; Early initialization settings.

;;; Code:

(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)

;;; early-init.el ends here
